name: speechless
description: An app for the BWKI 2022
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.0
  cupertino_icons: ^1.0.2
  translator: ^0.1.7
  path_provider: ^2.0.11
  flutter_sound: ^9.2.13
  permission_handler: ^10.0.0
  audio_session: ^0.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true